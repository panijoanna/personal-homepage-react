import { ThemeButton, ThemeWrapper, ThemeToggleIcon } from "./styled";

const ThemeSwitcher = () => {

  return (
    <ThemeWrapper>
      <ThemeButton>
        <ThemeToggleIcon />
      </ThemeButton>
    </ThemeWrapper>
  );
};

export default ThemeSwitcher;
