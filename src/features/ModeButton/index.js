import { ModeCircle, ThemeButton, Toggle } from "./styled";

const ModeButton = () => {
    return (
        <ThemeButton>
                <Toggle/>
        </ThemeButton>
    );
};

export default ModeButton;